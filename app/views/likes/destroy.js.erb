if (<%= @like_index %> == true) {
    $("<%=@id_name%>").remove();
}else{
    $("<%=@id_name%>").html("<%= escape_javascript(render('shared/like', unliked_music: @unliked_music, like_index: false)) %>");
}